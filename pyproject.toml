[project]
name = "cv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12",
    "fastapi>=0.136.3",  # apply_tracker web UI
    "google-api-python-client>=2.169.0",  # apply_tracker gmail_sync
    "google-auth-httplib2>=0.2.0",  # apply_tracker gmail_sync
    "google-auth-oauthlib>=1.2.0",  # apply_tracker gmail_sync
    "langchain-core>=0.2",
    "langchain-google-genai>=4.2.1",
    "langchain-openai>=0.1",
    "loguru>=0.7",
    "lxml>=5.0",
    "pdfminer-six>=20221105",
    "pillow>=10.0",
    "playwright>=1.60.0",
    "pytesseract>=0.3",
    "python-dotenv>=1.0",
    "python-multipart>=0.0.30",  # apply_tracker web UI (fastapi Form)
    "python-telegram-bot>=21.0",
    "pyyaml>=6.0",  # Roles Registry (config/roles.yaml)
    "requests>=2.32",
    "selenium>=4.9",
    "textual>=8.2.7",  # apply_tracker tui
    "uvicorn[standard]>=0.48.0",  # apply_tracker web UI
    "webdriver-manager>=4.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]
