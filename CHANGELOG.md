- Corrected the toHero LangChain claim in `templates/lato/CV_PhD_en.tex`. The bullet said the pipeline was built from scratch "bypassing LangChain and LlamaIndex", while an enclosed recommendation letter that ships in the same PhD dossier states LangChain was used — a reader holding both documents saw the candidate contradicted by his own referee. Both statements are true in sequence, so the template now states the sequence: prototyped on LangChain, rewritten from scratch once the confidentiality constraint was pinned down. The canonical wording lives in `context/cv-rules/candidate_facts.md` §1a. Already-sent packages are deliberately not retro-edited.
- Made the DU diploma name identical across the French CV templates. It was spelled five different ways — `Diplôme d'Université (DU) Big Data & Data Science`, the same with `(obtenu)`, `Diplôme d'Université Big Data, Data Science et Analyse des Risques sous Python (DU, obtenu)`, and a bare `DU Big Data & Data Science` in two PROFIL blocks — so a reader comparing two of our own CVs saw two different diplomas. All are now `DU Big Data & Analyse du Risque`, keeping any existing `(obtenu)` marker.
- Removed the DU from the intérim (technicien) track entirely, and made its regression test real. `test_technicien_track_is_downlevelled_for_interim` asserted `"DU Big Data" not in tex`, but the template shipped `(DU) Big Data`, which does not contain that substring — so the diploma had been going out on that track the whole time while the suite reported it absent. The `Formations` entry is gone from `templates/lato/CV_Technicien_fr.tex` and the assertion now rejects every spelling (`DU Big Data`, `(DU)`, `Diplôme d'Université`, `Big Data`).
- Stopped the intérim CV from volunteering a driving-licence status. The PROFIL block ended with `--- sans permis B`, which answers a question the posting did not ask and can only narrow the match; the test that required that string now requires its absence instead.
- Restored the zero-width non-joiners that a previous pass stripped from the Persian comments in `templates/classic/CV_AI_fr.tex` and `CV_Python_fr.tex` (`حاشیه‌های`, `عنوان‌ها`, `می‌توانید`). They are comments, but a mangled ZWNJ is a mangled word.
- Resolved false positive finding classes (FP-5, FP-6, FP-7 in T-024 round 2) in the coherence gate (`src/pipeline/coherence.py`). (FP-5) Metric comparison is claim-qualifier aware: bounded/approximate phrases ("over N", "more than N", "plus de N", "~N", "roughly N", "environ N", "N+") match source numbers satisfying the bound while exact unqualified metrics are still flagged; (FP-6) R6 completeness is dossier-type aware: academic/PhD dossiers require email draft (email_draft.md) and do not require .txt cover letters, while job dossiers preserve full/short .txt requirements; (FP-7) L2 semantic pass receives enclosed supporting documents (e.g. recommendation letters, diplomas) truncated to 3000 chars per doc in prompt corpus so claims backed by enclosed evidence are not falsely reported unsupported. Added 6 unit tests in `tests/test_coherence.py`.
- Resolved false positive finding classes (T-024) in the coherence gate (`src/pipeline/coherence.py`). (1) R4 phone numbers (+33 6..., 06...) and ORCIDs (0000-0002...) are structurally excluded from metric extraction; (2) R4 LaTeX numeric decorations (`{,}`, `\,`, `~`, `\%`) are normalized before metric comparison; (3) R2 applied-for positions and PhD candidatures ("applying for the PhD position", "candidature au poste de doctorat") are distinguished from claimed credentials; (4) The evidence corpus checked by R2 and R4 is extended to include enclosed supporting documents (PDFs, TXT, MD, TEX in folder or subfolders like `context/`), with unextractable PDFs emitting an INFO-level finding. Added regression unit tests for all 4 fixes in `tests/test_coherence.py`.
- Made the coherence gate's Layer 2 semantic pass the default (owner decision 2026-08-03), replacing the `--semantic` opt-in with a `--no-semantic` opt-out. Opt-in was the wrong default for a safety gate: the layer costs $0 (one `agy` gemini-flash call), it is non-blocking, and it already no-ops when the `agy` binary is absent — so the only thing the flag protected was the owner remembering to type it, on precisely the runs where a missed contradiction is expensive. `check_dossier(semantic=...)` and `cmd_check` now default to `True`. `tests/test_coherence.py` gained an autouse fixture stubbing `_run_semantic_pass`, so the suite stays offline and deterministic regardless of the production default rather than depending on the old `False`. Documented the whole `main.py check` command in README, which had never described it.
- Repaired the coherence gate's Layer 2 semantic pass, which had never produced a single finding. Three independent defects, all verified against the `agy` CLI: the prompt was passed as a bare positional argument instead of behind `-p`; `--json-schema` was handed an inline JSON string when it takes a file path, and is rejected outright unless `--output-format json` is also set (`Error: --json-schema can only be used when --output-format is 'json'`); and the response was read as `data["contradictions"]` when agy returns an envelope whose payload sits under `structured_output`. Any one of these was enough to make the layer a no-op. It also consumed only `contradictions` while the schema requested `unsupported_claims` and `narrative_drift` as well, so two thirds of the answer was discarded; all three are now mapped to findings. Most importantly the failure was silent — a non-zero exit, a bad envelope or any exception left `findings` untouched, which for a layer that now runs by default is indistinguishable from a clean semantic review. Failures are recorded as an explicit LOW `semantic-unavailable` finding, so the report can never imply a review that did not happen. Timeout raised 30s → 180s (a real dossier is ~28k input tokens).
- Resolved false positive HIGH findings (R5 identity mismatch) in the folder-based coherence gate (`src/pipeline/coherence.py`). Fixed subject line parser to split on the LAST separator ('—', '--', ' - ') so position titles containing dashes (e.g. "Reconversion Consultant(e) SAP - Roanne") are extracted cleanly without truncating the position or misinterpreting the remaining title as company name. Improved candidate name extraction for short cover letters (`cl_short`) lacking contact headers to inspect the signature block. Updated CLI `main.py check` to default to stdout-only reporting without writing `COHERENCE.md` into owner-controlled target directories outside the repository.
- Fixed two defects that made a generated application contradict itself, both found on the Sopra Steria SAP Roanne file. (a) `altacv_renderer._section_profile` wrapped the profile summary in `{\small …}` with no `\justifying`, so every generated CV printed a ragged-right profile block while all five hand-written `templates/altacv/*.tex` justify it — the one paragraph a recruiter reads first was the one paragraph that did not match the template. (b) The pipeline wrote only the ≤1500-char `cl_short` text file next to the letter PDF; the full letter existed as PDF only. Portals that reject or truncate PDFs (France Travail among them) need the same letter as pasteable text, and hand-typing it is exactly how the .txt drifts away from the PDF — on the file above the .txt still opened with "Titulaire d'un Master" while the compiled letter had been rewritten to lead with the technician track. `_write_full_cover_letter_txt` now renders it from the same `cl_intro`/`cl_body`/`why_this_company` fields the `.tex` is filled from, and reads the per-application city back out of the letter `.tex` so the address block cannot disagree with the PDF. `ApplicationBundle` gained `cl_full_txt`.
- Stopped `positioning.max_experience_entries` from deleting the mandatory internship. The rule told the model to keep only the N most recent "professional position(s)"; with N=1 the model kept the six-month toHero internship and dropped the three years at NISOC — i.e. the actual work history — on a posting that matches on exactly that history. An internship is not a professional position: the tailoring rule now states that the internship/stage entry is always included and never counts towards the limit, so N=1 means "internship + one job".
- Fixed the header's last contact field wrapping to a second line with ~97pt of empty space beside it, boxed every technology in the owner's blue tag style by default, and made project repository links visible. Three defects in `altacv_renderer.py`, all reported from a rendered PDF. (a) altacv's `\printinfo` puts its 2em trailing separator *inside* the item's `\mbox`, so the last field on a line is measured as "text + 2em" and wraps even when the text alone fits — `amirshirali.com` (63.5pt wide) jumped to line 2 next to 97.2pt of free space. `\printinfo` is redefined with the separator outside the box as discardable glue (`1.6em plus .4em minus .35em`), which is what a separator should be. (b) `Technos:` lines were plain comma-separated text; a `\stacktag` macro (tikz, `EBF5FB` fill / `85C1E9` border, rounded) now boxes each technology individually in both Experience and Projects, enabled unconditionally rather than behind a flag. (c) project titles were already wrapped in `\href` when the profile carried a `url`, but with no colour, icon or underline the link was invisible — a reader could not tell a project was on GitHub. The title now carries an accent-coloured `\faGithub` plus the `owner/repo` path (`\faLink` and the bare host for non-GitHub URLs).
- Stopped treating an ATS's bot protection as a dead application link. The apply-URL guard fetched `posting.apply_url` with `requests.get` and raised a fatal `Application link is unreachable` on **any** status ≥ 400, so a live posting was declared expired and the whole LLM path was abandoned — `generate_with_llm_fallback` then silently produced an offline-dictionary CV with the wrong role and a `Company` folder name. This fires on a large class of employers: the Sopra Steria SAP Roanne posting redirects to SmartRecruiters' `oneclick-ui`, which returns HTTP 403 to every non-browser client, browser User-Agent included, because it needs a JS session. 401/403/429 are now logged as "could not verify" and generation continues; only 404/410 are fatal; other 4xx/5xx warn. The body is also no longer scanned for expiry phrases on an error response, since an error page containing "not available" was itself enough to trigger the expired-offer branch.
- Cleared the pre-existing ruff lint backlog and docstring SyntaxWarning across the codebase. Removed unused imports (F401) in archiver, audit, cv_renderer, draft, job_scraper, latex_builder, resume_parser, service, status, and web, removed unnecessary f-strings without placeholders (F541) in audit, job_discovery, stats_cli, and web, removed unused local assignments (F841) in audit and service, resolved ambiguous single-letter variable names (E741) in audit and sources, split inline semicolon statements (E702) in web, and fixed an invalid escape sequence '\p' SyntaxWarning in latex_builder by making docstrings raw strings.
- Stopped recording a recruiting intermediary as if it were the employer (wo-applyforge-0014). The pipeline read only the offer text and stored whoever published it as the company, so an offer posted by a recruitment agency, an ESN/SSII, a staffing firm or a job-board reposter was filed under the intermediary's name while the real employer — usually anonymised as "pour le compte de notre client, un grand groupe…" — was lost. Applications were sent believing the agency was the employer. A new `src/pipeline/employer_extractor.py` first screens the text cheaply (a name from `config/known_agencies.yaml`, or one of the French intermediation phrases such as `notre client`, `cabinet de recrutement`, `agence d'intérim`) and only then spends an LLM call to resolve `employer_type` ∈ {direct, agency, esn, staffing, jobboard, unknown}, `posting_via` and the real employer; a direct posting costs nothing extra. `employer_type` and `posting_via` are added to the `positions` table through the existing `ALTER TABLE` migration loop, and the tracker UI, HTML export and daily alert now render `Real Employer (via Intermediary)` so an intermediary can never be presented as the employer unmarked. The agency list is config, not code, and is whitelisted in `.gitignore` (which ignores everything by default) — untracked it would load as an empty list and silently reclassify every named-agency posting as `direct`, so a missing or empty list now logs a warning rather than degrading in silence.
- Made the years-of-experience policy data-driven and inverted its default, via a new `cv_evidence` block in the candidate vault file. The prompt previously told the model it "may state" any year figure present in the CANDIDATE PROFILE, which is the one thing a screening figure must never do: naming a total self-rejects the application against every posting asking for more, and it can oversell a candidate into an interview they then have to defend. The default is now silence — no duration may appear in `cv_summary`, `cl_intro`, `cl_body`, `cl_short` or `why_this_company` — and seniority is instead conveyed through `cv_evidence.scale`, a list of real quantified facts (flat, or per-language) the model may reuse verbatim and may not extend. The single exception is a posting that explicitly demands N years: `_posting_required_years` detects the figure in code (requiring it to sit within 60 characters of "expérience"/"experience", so a `CDD de 2 ans` contract length is not mistaken for a requirement), and `cv_evidence.countable_window` caps what may be claimed at the candidate's real continuous window, quoted with its dates as proof. Enforcement is not left to the prompt: `_strip_years_and_metrics` gained an `allowed_years` argument, so exactly that one figure survives post-processing and every other duration is still deleted — authorising "3 ans" cannot smuggle a "7 ans" through alongside it. `positioning.allow_stating_years` keeps its previous meaning for profiles that do want a career total.
- Fixed every `lato` spontaneous CV failing to compile. `build_spontaneous` copied only `.cls`/`.sty` files out of the template folder, but each lato template `\input{lato_macros}` on line 2 — a `.tex` file — so pdflatex aborted with `File 'lato_macros.tex' not found` and produced no PDF. The apply path already had the correct rule (copy `.cls`/`.sty`, plus any `.tex` that is not a `CV_*` template); the spontaneous path now shares it. This had made the whole `technicien` intérim track unusable end-to-end even though its template and role entry were already in place.
- Removed every hardcoded years-of-experience claim from the committed CV templates, and added a test that fails if one ever returns. Four templates asserted `plus de 7 ans d'expérience` (`CV_IT_fr`, `CV_IT_Infra_Lato`, `CV_Support_fr`) and one `7 ans d'expérience` (`CV_Product_Telephonie_fr`), while `CV_NetworkAutomation_en` claimed `over 5 years` — three different figures, none of which matched the candidate's actual history, all of them defeating `positioning.allow_stating_years: false` by asserting the number in the template instead of letting the LLM state it. A stated figure also self-rejects against every "N ans minimum" posting. The claims are replaced with unquantified phrasing; a hobby line (`10+ years of personal experience in stock markets`) is deliberately exempted, as is the gitignored third-party `templates/tina/`. Dropped the `("with 7+ years", "avec 7+ annees")` translation pair that existed only to carry the fabricated figure into French.
- Added a `technicien` role: the intérim / technicien-tier CV track. It is deliberately distinct from `support` rather than an alias, because `technicien_adapter` can only down-level a CV once it has detected the tier from a job posting — the `spontaneous` path has no posting, so an intérim application built from `support` still shipped the DU, the academic honours and the `Ingénieur` title, which is precisely what interim agencies screen out (an over-qualified candidate is assumed to leave mid-mission). `CV_Technicien_fr.tex` therefore carries the down-levelled baseline itself, orders skills networks-first, and states the availability signals the channel actually ranks on (shift work, astreinte, sédentaire-only given no driving licence).
- Replaced the `REDACTED-EMPLOYER` / `REDACTED-CITY` strings that the PII history purge left behind in 30 tracked files with the repo's own placeholder convention (`Company Name`, `City Name`); in Python they were only ever docstrings and comments, now phrased as behaviour ("the posting's city") rather than a specific city, and `job_stats_scraper.mjs` regains the correct département label `Isère (38)`. Also dropped the dead `(" at REDACTED-EMPLOYER", " chez REDACTED-EMPLOYER")` translation pair, which could never match real text and only existed because the employer name had been hardcoded in the repo.
- Made the `selected_experience` history limit data-driven. A new `positioning.max_experience_entries` config key in the candidate vault file allows limiting the tailored CV to the N most recent positions, rather than forcing the entire work history. The PhD role continues to include the full history regardless of this setting, as it builds a comprehensive academic CV.
- Fixed `_strip_years_and_metrics` silently deleting whole clauses of generated text — the single biggest reason every generated CV still needed hand-editing. Both years-of-experience regexes ended in a greedy `[^,.]*`, which swallowed everything up to the next comma or period even though the trailing `d'expérience` group was optional: `"3 ans de production critique sur des réseaux télécom en supervision 24/7"` was reduced to the empty string, and the owner's approved reconversion framing `"Ingénieur en reconversion avec 3 ans d'expérience en automatisation Python…"` was truncated to the dangling fragment `"Ingénieur en reconversion avec"`. Stripping a fabricated percentage also left ungrammatical French (`"Réduction de 70% des interventions manuelles"` → `"Réduction de des interventions manuelles"`). The patterns are now bounded to the quantified phrase itself and additionally consume an immediately-preceding dangling connector (`avec`/`with`/`de`/`d'`/`à`/`by`/`of`), so surrounding prose always survives intact and grammatical; genuine sourced metrics (`~80%`, and bare figures matching `known_metrics`) are still preserved. Every mutation now emits a `logger.warning` with the before/after, so silent content loss can never go unnoticed again. Added regression tests for all four proven-broken inputs.
- Made the CV city fallback and override data-driven. Replaced the hardcoded regional fallbacks with a `cv_location` block in the candidate vault file. Users can now configure a specific `city`, an `always_use` flag to unconditionally override the job's own location, and custom mobility phrases (`mobility_fr`, `country`) for the output formatting. If no city is configured, it falls back to the job's own location string. `is_occitanie` and `is_france_travail` are kept exported and functional. Added `tests/test_location_utils.py`.
- Made the tailoring prompt's `cl_body` instruction data-driven instead of naming the owner's personal projects. It hardcoded "Detail the 'Company Name' RAG and document analysis project" and "the multi-agent architecture projects ('ApplyForge', 'Su6i-Yar', or custom elevator project)" — three problems at once: it put personal project names into a tracked generic template (rule-035 / templates-must-be-data-driven), `'Company Name'` was a leftover sanitisation placeholder that literally instructed the model to describe a project by that name, and it forced every cover letter to be about RAG and multi-agent systems even for a network or industrial-technician posting, contradicting the prompt's own later rule to adapt to the actual job domain. Both paragraphs now select from the CANDIDATE PROFILE by relevance, with an explicit instruction to prefer infrastructure/network/automation items for non-software roles and never to name a project absent from the profile.
- Made the tailoring prompt's candidate positioning rules data-driven. The prompt no longer hardcodes specific job titles or biographical framing (which leaked one specific person's profile into the public repo). It now dynamically injects rules based on the new `positioning` block in the candidate vault file: `avoid_titles` prevents specific words in the tagline, `avoid_labels` prevents unwanted descriptive terms, `framing` offers a preferred candidate description, and `allow_stating_years` controls whether the LLM may state the exact years of experience found in the profile.
- Made `_parse_json` fail loudly instead of returning `{}` on a malformed LLM response: the caller then built a `TailoredContent` with `company_name="Unknown Company"` / `position_title="Unknown Position"` and the pipeline went on to render and LaTeX-compile a CV containing those strings. It now raises a new module-level `TailoringError(ValueError)` carrying the decode error and a truncated snippet of the raw output, matching the repo's established fail-loudly convention (cf. the `--licence` no-op fix).
- Purged the `templates/tina/` family from the entire git history and gitignored the folder: its three files (`CV_AI_Data_Tina.tex`, `CV_IT_Infra_Tina.tex`, `lato_macros.tex`) were not skeleton templates but fully filled personal CVs (real employer/`Company Name`, academic grades, personal project names, personal website) committed to the remote — a rule-035 personal-data leak with no exception. Unlike `lato`/`altacv`, where filled variants are kept untracked on disk, `tina/` had every file tracked. Rewrote all history with `git filter-repo --path templates/tina --invert-paths` (backup mirror + file copies preserved in the private vault); the real `.tex` files stay on disk only. Requires a force-push by the owner; GitHub may retain dangling blobs until the repo is re-pushed/recreated.
- Enforced the constitution's English-only rule for agent-facing files: translated the two Persian scraper skills (`scrape_cnrs_emploi.md`, `scrape_servicepublic.md`) to English and removed Persian docstrings / console strings / web-UI text from `job_discovery.py` and `web.py` (French job-posting signal phrases are kept verbatim — matching data, not prose). Consolidated the duplicated "no Persian in CVs" rule into a single source of truth (`quality_guard.md`), generalized to "one target language per generated document", with a pointer from `project-rules.md`. Moved `README.fa.md` under `docs/fa/`, linked by a single Persian word from `README.md`.
- Made the pre-generation eligibility screening data-driven instead of hardcoding the candidate's personal attributes: nationality / driving-licence / civil-servant status were baked into `service.py`'s blocker list (a public-repo privacy leak, rule-035 violation). They now live in a private, gitignored vault file (`<vault>/data/candidate.yaml`; committable template `config/candidate.example.yaml`); `src/core/candidate.py` loads it and `eligibility_blockers()` emits only the blockers a given profile cannot satisfy (e.g. a candidate with a licence no longer blocks "permis B obligatoire" offers), with generic messages. Relocated the candidate-specific rules (`cv_layout_tuning.md`, `no_driving_license.md`) out of `.agent/local-rules/` to the vault and genericized `agent_guide.md`/`eligibility_screening.md`. Added `tests/test_candidate_eligibility.py`.
- Fixed the French cover letter header repeating the mobility phrase (e.g. "Montpellier, mobile en France, Mobile en France"): `_build_cover_letter` started applying the same job-aware city override as the CV (see the France Travail/Montpellier fix below), but `select_cv_city`'s French branch already bakes "mobile en France" into `\cvlocation` — the `Cover_Letter_Template_Fr.tex` header still appended `\cvmobilityFR` separately on top of that. The English template was unaffected (its branch of `select_cv_city` returns a bare "City, France"). Removed the redundant macro from the FR header line.
- Fixed the root cause behind the `--licence` no-op (see below): `resume_loader.generate_role_profile()` — the LLM function that (re)builds every role's derived profile from the master CV — had an output schema that never listed `conditional_education` at all, so the field was silently dropped from all 12 derived profile files (AI/DevOps/PhD/Python/Support × en/fr × source/plain), not just Support. Added the field to the prompt schema/rules, and — since a prompt instruction alone can drift again later without anyone noticing — added `_carry_over_conditional_education()`, which force-copies it from the master CV JSON into the parsed output after the LLM call, regardless of what the LLM actually returned. Added `tests/test_resume_loader_licence.py`. (The 12 vault data files themselves were backfilled on disk, not through this repo — vault data isn't version-controlled here.)
- Fixed `--licence` silently no-op'ing when a role's derived profile has no `conditional_education` field (discovered on the Support role after `--licence` produced a CV missing the Bachelor's degree with no indication anything was wrong). Extracted the injection into `_resolve_licence_education()`, which now raises `LicenceProfileMissingError` when there's nothing to inject. `generate_with_llm_fallback` catches it before the generic exception handler and returns immediately rather than silently retrying via the offline path (which doesn't support `--licence` at all) — `main.py` already turns a `None` bundle + error message into a clean non-zero exit, so no CLI changes were needed beyond also skipping the offline-retry prompt when `--licence` is set. Added `tests/test_service_licence.py`.
- Fixed the DU (Diplôme Universitaire) degree and full Master's honors being silently dropped from a Cadre-tier CAT-AMANIA DevOps application: `job_scraper._parse_html` was including France Travail's "D'autres offres peuvent vous intéresser" (related-offers sidebar) in the scraped body, and one unrelated suggested listing happened to be titled "Technicien système (H/F)" — that single keyword match falsely triggered `technicien_adapter.is_technicien_tier()`, which then dropped the DU degree and filtered Master honors down to network/systems-only, even though the actual posting's own "Qualification" field said "Cadre", not "Technicien". Added `_strip_trailing_noise()`, which cuts the scraped body at known "related offers"/generic-help-boilerplate markers before it reaches any downstream keyword scan or the LLM — also reduces token cost. Added `tests/test_job_scraper.py` regression tests. (Initially assumed a second France-Travail posting, Lùkla, was *correctly* detected as technicien-tier since its own "Qualification: Technicien" field is genuine and predates the noise boundary — wrong call, corrected below: that field is an HR pay-grade label, not a description of the job's actual requirements.)
- Fixed `technicien_adapter.is_technicien_tier()` triggering on France Travail's structured "Qualification : Technicien" field (an administrative/collective-bargaining pay-grade classification) even when the posting is genuinely engineering-level work (Lùkla's "DevOps Junior" — Ansible/Terraform/Docker, no Bac+2/3 requirement anywhere in the actual description) — silently dropped the DU degree and filtered Master honors down to network/systems-only for a role that never asked for that. `technicien`/`technicienne` in the body is no longer sufficient on its own; it's now only checked in the job TITLE (a direct signal), while body-wide matching requires a signal that actually describes the required level (`bac+2`, `bac+3`, `catégorie b`, `assistant informatique`, `aide-technicien`). Added `tests/test_technicien_adapter.py` covering both false positives found this session (the Qualification-field one and the related-offers-noise one) plus the still-valid true-positive cases. (Adding that test file surfaced a `.gitignore` footgun: an unanchored `test_*.py` rule meant for repo-root scratch scripts was also silently shadowing legitimate files under `tests/` — anchored to `/test_*.py`.)
- Fixed the CV/cover-letter location always showing "City Name" for jobs sourced from France Travail, where the owner is registered under Montpellier as his jobseeker domicile — regardless of the actual job's city, that registration must stay consistent. Two bugs found together: (1) `select_cv_city`/`_inject_location_override` only looked at the job's own city (Occitanie region → Montpellier, else City Name) with no concept of the posting's source — added `is_france_travail(job_url)` (checks `francetravail.fr`/`pole-emploi.fr` domains) which now wins over the per-job-city heuristic. (2) The `altacv` template's header never actually used the `\cvlocation` macro this whole mechanism patches — it prints a literal `\location{...}` baked in from `identity.location` at render time in `altacv_renderer.py`, so the override was silently a no-op for every altacv CV (the default template). `_build_cv` now overrides a copy of the profile's `identity.location` directly for non-lato templates. Also: the cover letter never had *any* per-job location override before now — it always used personal_data.tex's static default; `_build_cover_letter` now applies the same override.
- Added a plain-text short cover letter (≤1500 chars), generated alongside the PDF for every `apply` run: many application portals (e.g. France Travail's "Lettre de motivation" field) cap pasted text at 1500 characters, so the LLM now also returns a standalone `cl_short` (greeting + hook + one concrete achievement + closing, no LaTeX). `_write_short_cover_letter` in `latex_builder.py` writes it as `<slug>-LettreMotivation_Courte_<role>_<lang>.txt` next to the PDFs; a hard `_enforce_char_limit` safety net truncates at the last sentence boundary if the LLM overruns the limit anyway.
- Fixed a regression of the "~80%" metric-stripping bug (previously "fixed" by only stripping bare percentages, see below): that fix assumed the LLM would always reproduce the literal `~` marker when quoting a sourced metric, but cover-letter prose naturally drops it while paraphrasing (e.g. rewriting "~80%" as "80%"), so the genuine figure was still deleted as if fabricated — found live while generating a CMS4P application. `_strip_metrics_in_summary`/`_strip_years_and_metrics` now take the candidate profile text handed to the LLM, extract every real sourced percentage from it, and keep any output number matching one of those — independent of tilde presence. Also added an explicit "keep the employer name consistent" rule to the tailoring prompt after a posting with two names (legal entity + brand) produced a letter that switched between them mid-paragraph.
- Fixed a hardcoded personal name in the Apply Tracker's HTML output (`generate_html.py`, `web.py` footers/titles) discovered during a git-history PII scrub (wo-applyforge-0011): now reads `OWNER_DISPLAY_NAME` from `settings.py` (vault `.env`), defaulting to `CV_OWNER_SLUG` with underscores/dashes turned into spaces if unset. Same pattern as the earlier `ALERT_TO_EMAIL` fix.
- Moved the `apply_tracker` package (web dashboard, TUI, Gmail sync, SQLite tracker, drafting, audit) here from `amir-cli` (wo-applyforge-0007): it depended on this repo's CV-generation pipeline, so `amir-cli` stayed a thin wrapper while owning tracker code duplicated the dependency. Now lives in `src/apply_tracker/`, imports rewritten to the `src.` layout convention. Added `fastapi`, `uvicorn[standard]`, `textual`, `python-multipart`, `google-api-python-client`, `google-auth-httplib2`, `google-auth-oauthlib` to `pyproject.toml` (all pre-existing amir-cli deps, carried over with the code — not new capability). `amir apply web/tui/sync/stats/alert` and `amir phd`/`amir job` in amir-cli now shell out here via `uv run python -m src.apply_tracker.<module>`, mirroring the existing `amir apply <url>` → `main.py apply` wrap. Data (tracking.json, tracker.db, vault) is untouched — no migration. Added `tests/test_apply_tracker.py` (22 tests, tmp-dir based, no real vault/Gmail needed).
- Fixed `uv run main.py test` giving false negatives on a correctly configured install: it only checked `OPENAI_API_KEY`, so a DeepSeek-only setup reported "NO", and it only checked the legacy `data/resume_profile.json`, so a vault-based install with role source profiles reported "NOT FOUND". It now resolves the LLM provider the same way `llm_factory.py` does (DeepSeek key wins if set, else OpenAI, Gemini as an independent fallback) and lists per-role status against the vault's `{slug}-CV_<Role>_source.json` files. Added `tests/test_main_diagnostics.py`.
- Fixed PDF job-posting URLs (e.g. a direct `.pdf` link) being scraped as raw HTML: `requests`+BeautifulSoup mojibaked the binary PDF bytes, producing garbage text that reached the LLM. `job_scraper.py` now detects a PDF response by `Content-Type` or magic bytes and extracts real text via `pdfminer.six`; if extraction fails or yields no usable text, it raises `JobScrapeError` to abort the application before any LLM call. Added `tests/test_job_scraper.py` (local PDF fixture, no network).
- Fixed the altacv skills section rendering every category with the same icon (`\faCode`): `_section_skills` now maps each canonical skill category to a distinct FontAwesome5 icon (`ai_ml`→robot, `mlops_devops`→cloud, `networks_support`→network, `data`→chart-line). Added `tests/test_altacv_renderer.py`.
- Fixed a bug where `~`-prefixed approximate metrics (e.g. `~80%`) in LLM-generated cover-letter/CV text lost their number: the anti-hallucination metric-stripper matched the digits regardless of the `~` marker, leaving a bare tilde in the rendered PDF. It now only strips bare/fabricated percentages, preserving sourced approximate figures. Added `tests/test_content_tailor.py`.
- Rewrote `install.sh` as a zero-touch, idempotent bootstrap (per the `bootstrap-installer` skill): OS detection, `uv` + LaTeX/tesseract, `uv sync`, personal-data vault creation with seeded `secrets/.env`, and an end import check. Replaced the constitution **git submodule** with a central-clone **symlink** (`.agent/constitution`, gitignored) so the rules/skills are a single source of truth with no per-repo SHA pinning. The installer also registers the constitution's skills into `~/.claude/skills/` so they appear in `/skills`, **version-safe**: it never overwrites a skill another project registered at a newer `version:`. `.env` now loads from the vault (`settings.py`).
- Removed the stale `.agents/` directory (pre-router-pattern layout). All content now lives in `.agent/`: `rules.md` → `local-rules/project-rules.md` (R1–R13 superset), `agent_guide.md`/`quality_guard.md` are byte-identical in `local-rules/`, and `cv-latex-workspace.md` is newer in the constitution submodule. Dropped its dead `.gitignore` reference.
- Reconciled the local branch with `origin/main` (two divergent post-`filter-repo` histories with no common ancestor). The local line is the newer superset — its roles-registry and external-vault refactors supersede the remote's hardcoded role/template maps and in-repo `data/` paths. Merge kept local content (`-X ours`); remote history is preserved as an ancestor so the push fast-forwards. Removed four templates the merge re-introduced under their pre-rename names (exact duplicates of `CV_AI_{fr,en}`, `CV_DevOpsAlternance_fr`, `CV_PhD_en`). Kept two distinct lato designs (`CV_AI_Data_Lato`, `CV_IT_Infra_Lato`) pending wiring into the registry.
- Roles Registry (`config/roles.yaml`): single source of truth for CV tracks. Canonical roles: `python` (generalist), `polyvalent`, `ai`, `devops`, `devops_alternance`, `phd`, `support` (network/support technicien — the former `it` track). **No aliases** — a role is its canonical key only. Role classifier, label maps, CV/spontaneous template maps, cover-letter variant, and skill ordering all derive from this file; new roles can be auto-scaffolded via `src.core.roles.scaffold_role`. Curated `CV_IT` profile renamed to `CV_Support`. CV templates standardized to `CV_<Label>_<lang>.tex`. Added `tests/test_roles.py`.
- PhD applications: generate a comprehensive multi-page academic CV (ignore the one-page limit; include all research / teaching / professional experience). Other roles keep the one-page convention.
- Pipeline: multi-paragraph cover letter (merged "why company" + closing), robust project URL recovery, title-aware technicien-tier detection, and role-relevant certification filtering
- Fixed French CV layout (switched language/certificates, strictly aligned left/right columns, and limited projects to fit in 1 page)
